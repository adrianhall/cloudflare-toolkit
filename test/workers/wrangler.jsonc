{
  // Minimal Worker configuration used only by the `workers` Vitest project to boot workerd via
  // @cloudflare/vitest-pool-workers. `worker.ts` is an inert stub.
  "$schema": "../../node_modules/wrangler/config-schema.json",
  "name": "cloudflare-toolkit-test-workers",
  "main": "worker.ts",
  "compatibility_date": "2026-07-10"
}
